## Goal
Scanner tab real pipeline: Android device camera + on-device MediaPipe (hands+pose) -> 441-dim feature vectors streamed to /api/v1/stream -> live server predictions rendered back. Target device: SM S731B (Android 16).
## Now
ARTIFACTS RESTORED + REAL-MODEL E2E VERIFIED 2026-07-11 (later same day): user dropped tsl_lstm_f32.tflite (1.6MB) + label_map.json (150 classes, NO idle class) + preprocess_config.json into Inference_backend/TSL_Output. Real config exposed key-name drift in the reconstructed tsl_preprocess.py -> renamed to training keys (hand_local_norm/hand_scale_norm/use_velocity/use_acceleration; default threshold now 0.6) and implemented hand_scale_norm (ASSUMED formula: wrist-recenter then divide by max landmark distance from wrist — original module still lost; live signing accuracy is the acceptance test). Idle bypass made safe for idle-class-less label maps (empty word + confidence 0 + is_idle, never class-0-at-100%). Rebuilt Inference_backend/.venv-x64 (grpcio 1.82.1, numpy 2.5.1, ai-edge-litert; dev.ps1 auto-prefers it). VERIFIED: ruff clean + pytest 47 passed; real-model gRPC e2e (server under .venv-x64): GetTuning model_loaded/150 classes/window 30/features 441/threshold 0.6; 35 random frames -> 6 predictions, is_uncertain (top-1 0.226 < 0.6), real Thai top-5, inference ~4.4ms; idle frames -> is_idle + empty word. PENDING: live signing accuracy on device (formula assumption); original tsl_preprocess.py from training repo would remove the assumption.

INFERENCE BACKEND REBUILT 2026-07-11: the entire ai/ tree (engine, server, pb stubs, tsl_preprocess.py, TSL_Output artifacts, .venv-x64) was lost in the Git repo separation; only tsl_live_inference.py survived at root. Rebuilt as Inference_backend/ (user's chosen name; root Agents.md/CLAUDE.md/docs repointed): tsl_preprocess.py is a SPEC-BASED RECONSTRUCTION (exact training hand-norm formula unrecoverable — user will copy the original + artifacts from the training repo later; module carries a replace-me notice); inference/engine.py (model lifecycle + hot-swap, per-stream sessions, idle bypass ported from tsl_live_inference.py, runtime tuning); inference/server.py (gRPC: StreamInference, UploadModel client-streaming with staged uploads/<ts>/ + active_model.json manifest, StreamLogs via inference/logstream.py ring buffer + subscriber queues, Get/SetTuning); proto extended (docs/api/tsl_inference.proto) and BOTH stubs regenerated (protoc-gen-go v1.36.11 + protoc-gen-go-grpc v1.6.2 installed to ~/go/bin, driven by grpc_tools.protoc). tsl_live_inference.py moved into Inference_backend/. VERIFIED: ruff clean + pytest 45 passed (fake interpreter — tflite runtime not needed); Backend go vet clean + go test ok with regenerated stubs. ARTIFACTS STILL ABSENT: tsl_lstm_f32.tflite, label_map.json, preprocess_config.json (user copies later; TSL_Output/README.md documents the drop); e2e with a real model NOT verified. Workspace is currently NOT a git repository (.git lost in the split).

STAGE C code DONE 2026-07-08 (feature vector assembled + send wired). New feature_vector_builder.dart: buildPositionVector/buildFeatureVector byte-match ai/extract_keypoints.py:120-166 ([Pose 7*3|Left 21*3|Right 21*3]=147, body-normalized by shoulder mid/width, z included, defaults mid=[0.5,0.5,0]/w=1 when no pose, hands normalized even without pose, missing->zeros); padded to 441 with zero vel/accel (server recomputes, reads first 147). landmark_extraction_service.dart: new parseFullFrame + frameStream on both services (Simulated emits the sim hand as rightHand; MediaPipe emits parseFullFrame per native event). scanner_provider.dart: _frameSub streams buildFeatureVector(frame) -> streamService.sendVector() while scanning (no-op in Simulated; WebSocket serializes 441 floats as landmark_frame). VERIFIED: flutter analyze clean, flutter test +26 (8 new: 5 buildPositionVector/buildFeatureVector layout + 3 parseFullFrame). Rebuilt+installed to SM S731B this turn. SERVER e2e VERIFIED 2026-07-08: local WS probe (scratchpad/ws_probe.py) sent 40 landmark_frames -> 1 ready + 11 prediction frames (first at seq 30 when the window fills; random input correctly flagged is_uncertain, top-5 Thai labels, inference ~5.7ms). Stack: Go gateway :8080 (ws://.../api/v1/stream) -> gRPC localhost:50051 -> Python LSTM. AI server MUST run under the x64 venv ai/.venv-x64 (ai-edge-litert/tensorflow have NO win_arm64 wheels; arm64 python raised ImportError). PC LAN IP 192.168.30.2. PENDING phone side: set Settings serverUrl=ws://192.168.30.2:8080, demo-mode OFF, Connect, sign -> confident predictions (Windows Firewall may block inbound :8080; preprocess_config.json still absent -> engine defaults, accuracy caveat).

STAGE B COMPLETE (B1+B2+B3). B3 DONE 2026-07-08 (native MediaPipe pipeline): CameraX ImageAnalysis (RGBA_8888, KEEP_ONLY_LATEST) on a single analysis thread runs HandLandmarker(numHands=2)+PoseLandmarker(numPoses=1) in RunningMode.VIDEO via detectForVideo; frames emit {pose:[x,y,z*33]|[], hands:[{handedness,landmarks:[x,y,z*21]}]} over LandmarkStreamHandler -> EventChannel 'signmind/landmarks'. .task models bundled in android/app/src/main/assets (noCompress "task"); dep com.google.mediapipe:tasks-vision:0.10.14. Dart: landmarkExtractionServiceProvider now returns MediaPipeLandmarkExtractionService on Android (Simulated elsewhere/tests, base points renormalized to 0..1); overlay painter maps normalized 0..1 across the full viewport (Positioned.fill). VERIFIED: `flutter build apk --debug` compiles (native MediaPipe/CameraX signatures type-check), `flutter analyze` clean, `flutter test` +18. Rebuilt+installed to SM S731B this turn. PENDING user on-device verify: real hand skeleton tracks the hand in the preview. NEXT: Stage C (147->441 vector from the emitted pose+hands, wire streamService.sendVector()). Caveats logged in Open items (preview crop vs full-frame overlay alignment; RGBA tight-pack assumption; pose emitted but not yet consumed).
### B2 history
B1 DONE + B2 DONE (2026-07-07, compiles). B2: native CameraX back-camera preview as PlatformView 'signmind/camera_preview' (MainActivity->FlutterFragmentActivity, CameraPreviewFactory/View, LandmarkStreamHandler EventChannel), CameraX deps in app/build.gradle.kts; Flutter shows AndroidView on Android and cameraControllerProvider returns null on Android (no double camera-open). Verified analyze clean + test +18 + `flutter build apk --debug` OK. B2 BLEED BUG (camera showed on Settings tab) fix applied 2026-07-07: (1) PreviewView.ImplementationMode.COMPATIBLE = TextureView not SurfaceView; (2) Flutter hybrid composition via PlatformViewLink+initExpensiveAndroidView; (3) bottomTabIndexProvider gates the preview to scanner tab only (shell IndexedStack keeps all tabs alive). Native camera-switch wired 2026-07-07: MethodChannel 'signmind/camera' setLens {back|front} -> CameraPreviewView.setLens rebinds CameraX; flip button now shown on Android (was hidden because it was gated on the disabled camera-plugin controller). Front is preview-only (mirrors handedness). Built+installed to device (--debug). PENDING user re-verify: camera clipped inside scanner viewport, no bleed on other tabs, flip button works. Landmark provider still Simulated (flip in B3). Next: B3 MediaPipe HandLandmarker+PoseLandmarker in ImageAnalysis -> emit landmarks -> flip provider -> align painter.
## Next
Staged; each stage ends with its own verifiable check. Client extraction MUST byte-match ai/extract_keypoints.py + ai/tsl_live_inference.py (spec in Facts). Currently ALL on-device pieces are simulated and scanner_provider.dart NEVER calls sendVector().
- Stage A — DONE 2026-07-07 (real back-camera preview; see Done).
- Stage B — DONE 2026-07-08 (native MediaPipe hands+pose over EventChannel; see Now). Original scope note below.
- Stage B (original) — Native MediaPipe (Kotlin platform channel): CameraX ImageAnalysis -> HandLandmarker(num_hands=2) + PoseLandmarker(num_poses=1) LIVE_STREAM using ai/*.task copied to android/app/src/main/assets; raw landmarks (pose 7-of-33 + z, hands 2x21 + handedness) over EventChannel -> real LandmarkExtractionService replacing the simulated one. CHECK: real hands/pose drive the overlay on device.
- Stage C — CODE DONE 2026-07-08 (see Now): 147->441 vector built + sendVector() wired + unit tests (+26). REMAINING: live e2e — run backend+AI, point Settings at it, confirm real `prediction` frames end-to-end (user/integration step; not automatable from the frontend package alone).
## Constraints
Please dont use the Absolute path please make it Workspace\Agents.md or smth like that instead.
Plan only for now (2026-07-07) — LIFTED same day; user said "Start Stage A". Stage A implementation in progress.
## Decisions
Reference CLAUDE.md in AGENTS.md under Core Contract and Read Before Editing sections.
Used Material wrapper inside Settings card containers to ensure ListTile ink splashes and background paint properly without throwing assertions.
DECISION: Compute the 147 position vector in Dart, not Kotlin — unit-testable via `flutter test`; keeps the native layer to landmark extraction + preview only.
DECISION: Use the BACK camera — MediaPipe handedness matches training only on a NON-mirrored feed; UI already labels "กล้องหลัง". Front/selfie would flip Left/Right hand blocks vs training.
DECISION: Native preview via CameraX inside a PlatformView (AndroidView) with the Flutter skeleton CustomPaint overlaid; ONE native camera session feeds both preview and MediaPipe analysis (avoid double camera-open with the `camera` plugin — may drop `camera` in Stage B in favor of the native session).
## Facts
- Root AGENTS.md: Workspace\AGENTS.md
- Guardrails config: Workspace\CLAUDE.md
- Flutter verify: `cd frontend && flutter analyze && flutter test`
- Go backend verify: `cd backend && go vet ./... && go test ./...`
- Python AI verify: `cd ai && ruff check && pytest`
- Device: SM S731B, Android 16 (API 36), arm64, wireless ADB. Dev host: Windows arm64.
- Artifacts RESTORED 2026-07-11 in Inference_backend/TSL_Output/: tsl_lstm_f32.tflite, label_map.json (150 classes, no idle class), preprocess_config.json (hand_local_norm+hand_scale_norm+use_velocity+use_acceleration true, threshold 0.6). MediaPipe .task files also in Frontend/android/app/src/main/assets/. Still lost: extract_keypoints.py, training scripts, original tsl_preprocess.py.
- Extraction spec (source of truth: ai/extract_keypoints.py:120-166, mirrored ai/tsl_live_inference.py:213-285). Per frame -> 147 position dims = [Pose 7*3 | LeftHand 21*3 | RightHand 21*3].
  Pose = MediaPipe pose indices [0,11,12,13,14,15,16] (nose, L/R shoulder, L/R elbow, L/R wrist).
  Body norm: shoulder_mid=(pose[11]+pose[12])/2; shoulder_width=||pose[11]-pose[12]|| (3D); every pose+hand xyz = (kp-shoulder_mid)/shoulder_width, z INCLUDED. No pose detected -> shoulder_mid=[0.5,0.5,0], width=1.0, pose block stays zeros.
  Hands assigned by handedness.category_name 'Left'/'Right'; missing hand -> zeros. Uses raw MediaPipe normalized x,y,z.
- Wire: /api/v1/stream schema_version 1 requires EXACTLY 441 floats [pos147|vel147|accel147]; server (internal/stream + ai/inference/engine.py) uses ONLY the first 147 and recomputes vel/accel + hand-norm itself, so client vel/accel are diagnostic padding (zero-fill or compute from a 2-frame history).
## Done
- Stage B1+B2 (2026-07-07) — RESULT: B1 Dart MediaPipeLandmarkExtractionService + parsePrimaryHand + 4 tests (provider still Simulated). B2 native CameraX preview PlatformView (MainActivity/CameraPreviewFactory/CameraPreviewView/LandmarkStreamHandler.kt + camerax 1.3.4 deps), Flutter AndroidView on Android + camera-plugin disabled on Android. Verified: analyze clean, test +18, `flutter build apk --debug` OK. On-device visual PENDING.
- Stage A camera preview (2026-07-07) — RESULT: added camera ^0.11.0 (resolved 0.11.4) + CAMERA permission; new cameraControllerProvider (FutureProvider<CameraController?>, null on no-plugin/permission/hardware); camera_viewport.dart -> ConsumerStatefulWidget renders cover-fitted CameraPreview over the gradient; new camera_provider_test. Camera-flip button added (Icons.cameraswitch, top-right; selectedCameraLensProvider Notifier — front camera is PREVIEW-ONLY, mirrors handedness vs training). Verified: analyze clean, test +14, `flutter build apk --debug` OK. On-device visual pending user.
- Add to AGENTS.md to read CLAUDE.md — RESULT: 2 lines added under Core Contract and Read Before Editing; verified via git diff.
- Add Setting page — RESULT: Created settings_models.dart, settings_provider.dart, settings_screen.dart, wired /settings route and gear icon button into scanner header and main theme mode; verified via flutter analyze and flutter test (3/3 suites passing).
- Stream contract — RESULT: docs/api/stream-schema.md (schema_version 1) + docs/api/tsl_inference.proto created.
- Go gateway — RESULT: cmd/server main, internal/config, internal/httpapi (RFC7807), internal/stream (WS handler + gRPC AIClient + 5 tests), internal/pb generated; verified via go vet ./... && go test ./... (ok internal/stream).
- Flutter server URL setting + real WS client — RESULT: serverUrl/useSimulatedStream settings persisted via shared_preferences, Server section UI, WebSocketTslStreamService + provider selection, tests updated/added; verified via flutter analyze (clean) && flutter test (+10 all passed).
- Python gRPC inference server — RESULT: inference/engine.py (window/idle/uncertainty), inference/server.py (bidi servicer), inference/pb stubs generated from proto; verified via ruff check (clean) + pytest (11 passed) + `from inference import server` import OK. grpcio 1.81.1 + protobuf 6.33.6 installed.
- Final cross-project verify (2026-07-07) — RESULT: backend `go test -count=1 ./...` -> ok internal/stream 0.816s; ai `pytest` -> 11 passed; frontend `flutter test` -> +10 all passed; all `analyze`/`vet` clean.
- WS crash fix + Connect button (2026-07-07) — RESULT: `ConnectionStatus` enum added to scanner_models.dart; `WebSocketTslStreamService`/`SimulatedTslStreamService` expose `connectionStatus`, and `channel.ready` errors (previously unhandled -> crash) now resolve to `disconnected` via `_teardownChannel()`; `tslConnectionStatusProvider` added for lightweight UI access; `ScannerNotifier` tracks status in `ScannerState`; `SettingsScreen` converted to `ConsumerStatefulWidget` with a `TextEditingController` + "Connect" button (`Key('connectServerButton')`) so typing no longer calls `setServerUrl`/reconnects per keystroke, and the server status card reflects real connection state; verified via `flutter analyze` (clean) + `flutter test` (+12 all passed, including 2 new regression tests proving the invalid-URL path no longer throws).
## Open items
- B3 overlay alignment: PreviewView uses FILL_CENTER (crops) while MediaPipe landmarks are normalized to the FULL analysis frame; the skeleton tracks the hand but may not pixel-align with the cropped preview, and normalized x/y are stretched onto the viewport aspect. Refine in a later pass if alignment looks off (match PreviewView scale-type math or letterbox the overlay).
- B3 RGBA tight-pack: `toUprightBitmap` copies planes[0] assuming rowStride==width*4 (per the MediaPipe Android sample). If a device reports row padding the image will skew; handle rowStride/pixelStride if that shows up.
- Stage C sends EVERY MediaPipe frame to the server (mirrors tsl_live_inference.py); the FPS/temporal caveat below still applies — the server windows 30 frames, tune send-rate if predictions look off.
- Stage C client vel/accel are zero-filled diagnostic padding; server recomputes from its own history. If a future server build ever reads client vel/accel, compute them from a 2-frame history instead.
- B3 front camera: analysis is NOT mirrored (correct for handedness), so front stays preview-only per the BACK-camera decision. FIXED 2026-07-08: the overlay painter mirrors x (1-x) when the front lens is active so the skeleton lines up with the mirrored front preview (_HandSkeletonPainter.mirror, gated on selectedCameraLensProvider==front). Verified analyze+test+18; user confirmed the flip was on the FRONT lens.
- 16 KB page-size warning on device (Android 16, SM S731B): bundled prebuilt .so libs (libmediapipe_tasks_vision_jni.so LOAD-segment alignment, plus libflutter/libdartjni/CameraX util libs) are not 16 KB-aligned -> non-fatal "app compatibility" dialog, dismissable, app runs fine. Only blocks Play Store upload. Fix later: bump tasks-vision to a 16KB-aligned build (>=0.10.20) and a Flutter engine that ships aligned libs. NOT done.
- backend/server.exe stale binary committed in repo — consider deleting/gitignoring (NOTED, not done).
- Model artifacts RESTORED 2026-07-11 (tflite + label_map + preprocess_config in Inference_backend/TSL_Output). tsl_preprocess.py remains a reconstruction: config KEYS now match training, but the hand_scale_norm FORMULA is assumed (max-distance-from-wrist). Verify by live signing accuracy; replace with the original module if ever recovered. Label map has NO idle class -> engine returns empty word on idle bypass.
- FPS/temporal caveat: training subsampled to ~TARGET_FPS 12 (extract_keypoints FRAME_STEP), but tsl_live_inference.py feeds every camera frame into the 30-window. Client send rate sets effective window fps; mirror the live script (send each processed frame, server windows 30) and tune if predictions look off.
- Stage B open question: bundle ai/*.task into APK assets (adds ~17MB) vs download-on-first-run. Decide at Stage B.
- camera_android_camerax applies legacy KGP -> Flutter build warning only (android.builtInKotlin=false keeps it working). Stage B native CameraX may replace the `camera` plugin outright.
- Stage A does NOT handle app-lifecycle pause/resume or mid-session camera disconnect (deferred to Stage B native session).
## Failed attempts
ATTEMPT 1 [L1]: background `pip install grpcio grpcio-tools` (no win_arm64 wheels -> source build) -> grpcio-tools wheel built+cached, grpcio build stalled after ~1.5h (0 CPU, no cl.exe); killed PIDs 12988/20828.
ATTEMPT 2 [L1]: rerun with GRPC_PYTHON_BUILD_EXT_COMPILER_JOBS=8 and -v logging -> was compiling normally (parallel cl.exe, log growing) but user paused the session; killed PID 23480 cleanly. Not a failure — resume with the same command.
NOTE: parallel `pip install --no-deps grpcio-tools` alongside the main build never completed (likely pip contention) — run installs one at a time.
ATTEMPT 3 [L2]: grpcio==1.81.1 build with jobs=8 -> `fatal error C1083: Cannot open compiler generated file: '': Invalid argument` on deeply nested upb-gen envoy path = MAX_PATH overflow in %TEMP%\pip-install-* tree. New hypothesis: build-root path too long, not a compiler/stall problem (likely also explains ATTEMPT 1's "stall"). Fix: TEMP=TMP=C:\t and rebuild.
ATTEMPT 4 [L3]: TEMP=C:\t rebuild -> compile passed the old failing file, but LINK failed: LNK1181 missing `client_side_weighted_round_robin.upb_minitable.obj` — longest path still ~256 chars (pip-install-<rand>\grpcio_<32hex> prefix ≈65 chars). LongPathsEnabled registry blocked (no admin). Fix: sdist extracted to C:\t\g, in-tree `pip install .` (deepest path ≈204 chars), log scratchpad/pip_grpcio4.log.
RESOLVED 2026-07-08: ATTEMPT 4 (short-root in-tree build) SUCCEEDED — grpcio 1.81.1 + grpcio-tools 1.81.1 now installed in Python312-arm64 site-packages. Verified `import grpc` (native ext loads), `from inference import server`, `ruff check` clean, `pytest` 11 passed. The MAX_PATH overflow was the real cause (not a stall); the short build-root (C:\t\g) fixed it. Do NOT rebuild — grpcio is functional.
